{
  "skillId": "windows-runtime-validation",
  "name": "Windows Runtime Validation",
  "description": "Native Windows runtime validation of the built desktop application: startup, IPC, process supervision, Job Objects, isolation, restart/recovery, credential storage, and installer/uninstaller behavior. Requires target_platform = windows AND a native Windows execution environment. On any other host it reports USER_REQUIRED or UNAVAILABLE and never a simulated pass.",
  "version": "1.0.0",
  "scope": "BUILT_IN",
  "compatibleWorkerRoles": ["validation_worker"],
  "triggerConditions": [
    "target_platform_is_windows",
    "stage_is_runtime_validation_or_certification"
  ],
  "requiredTools": [],
  "requiredCapabilities": ["windows_native_execution", "windows_native_ipc", "windows_process_supervision_recovery"],
  "permissionRequests": [],
  "inputSchema": {
    "type": "object",
    "properties": {
      "environmentId": { "type": "string" },
      "validationEnvironmentId": { "type": "string" },
      "artifactIds": { "type": "array" }
    },
    "required": ["environmentId", "validationEnvironmentId", "artifactIds"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "validationGateResult": { "type": "string" },
      "evidenceIds": { "type": "array" },
      "simulated": { "type": "boolean", "const": false }
    },
    "required": ["validationGateResult", "evidenceIds", "simulated"]
  },
  "sourcePath": "builtin/windows/windows-runtime-validation",
  "scanStatus": "CLEAN",
  "trustStatus": "TRUSTED",
  "enabled": true,
  "installedAt": 0,
  "lastUsedAt": null
}
